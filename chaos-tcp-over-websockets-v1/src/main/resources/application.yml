spring:
  application:
    name: tcp-over-websockets

# 隧道转发路径上 ByteBuf 的拷贝策略：copied | retained（默认，零拷贝且链路安全）
# 注意 duplicate 在异步转发链路中不安全，配置后会被强制回退为 retained
buf:
  copy:
    strategy: retained

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
